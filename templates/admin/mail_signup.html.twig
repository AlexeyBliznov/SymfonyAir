{% extends 'mail.html.twig' %}

{% block title %}Sign Up Confirmation{% endblock %}

{% block body %}

    <p>Sign Up confirm link:</p>

    {% set url = url('admin.register.confirm', {'token': token, 'email': email}) %}

    <p><a href="{{ url }}">{{ url }}</a></p>

{% endblock %}
